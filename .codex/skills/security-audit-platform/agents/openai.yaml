interface:
  display_name: "Security Audit (Platform)"
  short_description: "Deep platform-wide MCP Runtime security audit"
  default_prompt: "Use $security-audit-platform to run a deep, repository-wide security audit of MCP Runtime — threat model, authn/authz matrix, tenant isolation, protocol fuzz, audit-log integrity, TLS hygiene, DAST, and full scanner pass."
