interface:
  display_name: "QA — E2E Operations"
  short_description: "Real-cluster operator/CLI/setup/rollout regression checks"
  default_prompt: "Use $qa-e2e-operations to validate MCP Runtime operator, CLI, setup, registry, ingress, and rollout behavior against the live Kind contributor cluster (qa-cluster-bringup must have run)."
