apiVersion: v1
kind: Namespace
metadata:
  name: mcp-sentinel
  labels:
    # Promtail reads node /var/log/pods via hostPath; baseline/restricted block that volume type.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
